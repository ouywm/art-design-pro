/** 默认分片大小：10MB */
export const DEFAULT_CHUNK_SIZE = 10 * 1024 * 1024

/** 默认并发上传数 */
export const DEFAULT_CONCURRENCY = 3

/** 默认最大重试次数 */
export const DEFAULT_MAX_RETRIES = 3

/** 默认重试基础延迟（ms） */
export const DEFAULT_RETRY_DELAY = 1000

/** MD5 计算分块大小：2MB */
export const MD5_CHUNK_SIZE = 2 * 1024 * 1024
